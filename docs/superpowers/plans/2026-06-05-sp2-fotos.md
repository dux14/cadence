# Plan de implementación — SP2: Fotos (Cadence v2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to execute this plan. Read the whole plan first, then work task by task, committing after each. Do not batch tasks. Verify each command's expected output before moving on.

- **Fecha:** 2026-06-05
- **Spec:** `docs/superpowers/specs/2026-06-05-cadence-v2-design.md` (sección **SP2 — Fotos**)
- **Prerrequisito:** SP1 ya entregado (ver "Supuestos sobre SP1" más abajo).

---

## Goal

Añadir fotos a tasks, ideas y backlog en Cadence. El usuario puede adjuntar imágenes por:

1. **Picker de archivos** (`<input type="file" accept="image/*">`).
2. **Cámara móvil** (`<input type="file" accept="image/*" capture="environment">`).
3. **Paste de portapapeles** (evento `paste` con `clipboardData.items`) en el quick-add y en el sheet de edición.

Cada imagen se comprime client-side (sin dependencias nuevas) a WebP: blob principal con lado mayor ≤1600px (q≈0.8) y thumbnail con lado mayor ~200px. Objetivo medible del spec: **una foto de 4 MB queda ≤300 KB**.

En la preview de fila aparece un **chip único `Image` (lucide) + contador**. En el sheet hay un **grid de thumbnails**; al tocar uno se abre un **viewer a pantalla completa** con swipe entre fotos (móvil), flechas (desktop) y botón borrar (tombstone). Al **promover una idea a task** las fotos siguen al item (re-parent). Borrar la entidad padre tombstonea sus fotos. Los object URLs se revocan para no fugar memoria.

El binario **nunca** entra en la entidad ni en el payload de sync relacional: vive solo en la tabla `photos`.

## Architecture

- **Persistencia:** tabla Dexie nueva `photos` (`version(3)`), independiente de las entidades. Relación por `parentType` + `parentGuid` (no por id numérico, para sobrevivir al re-parent y al sync de SP4).
- **Capa de datos pura** (`lib/db/photos.ts`): helpers `addPhoto`, `listPhotos`, `tombstonePhoto`, `reparentPhotos`, `tombstonePhotosForParent`. Todo testeable con `fake-indexeddb`.
- **Pipeline de compresión** (`lib/image/compress.ts`): se separa en dos partes:
  - **Lógica pura testeable** `fitDimensions(w, h, max)` → cálculo de dimensiones de resize, 100% testeable en node/jsdom.
  - **Pipeline impuro** `compressImage(file)` → usa `createImageBitmap` + `OffscreenCanvas`/`<canvas>` + `canvas.convertToBlob`/`toBlob`. NO corre en jsdom; se valida con mocks unitarios mínimos y, sobre todo, con **verificación manual** (instrucciones en la Task final).
- **Gestión de memoria** (`lib/image/object-url.ts` + hook `usePhotoUrls`): crea object URLs y los revoca en cleanup de efecto/desmontaje.
- **UI:**
  - `components/photo-attach-button.tsx`: botón(es) de captura (picker, cámara) reutilizable en quick-add y sheet.
  - `components/photo-grid.tsx`: grid de thumbnails dentro del sheet.
  - `components/photo-viewer.tsx`: viewer fullscreen (Radix Dialog) con navegación y borrado.
  - `components/photo-chip.tsx`: chip `Image` + contador para la preview de fila.
  - Hook de paste `lib/use-paste-images.ts` enganchado en quick-add y sheets.
- **Integración:** `task-row.tsx`, `idea-row.tsx`, el row/sheet de backlog y `quick-add.tsx` consumen los componentes anteriores. El chip se inserta en la preview respetando el orden `[due][checklist][links][fotos]` que SP1 dejó montado.

## Tech Stack

- Next.js 16 (static export, `output: "export"`), React 19, TypeScript 5.
- Dexie 4 + `dexie-react-hooks` (`useLiveQuery`).
- Tailwind 4, `lucide-react` (**iconos siempre lucide, nunca emojis**).
- `@radix-ui/react-dialog` (ya en uso para sheets/modales).
- Serwist PWA (sin cambios en SP2).
- **Sin dependencias nuevas.** Compresión vía Canvas/`createImageBitmap` nativos.
- Testing: **vitest** + **fake-indexeddb** (configurados en SP1). Gestor: **pnpm** siempre.

## Supuestos sobre SP1 (adaptar nombres si difieren)

SP2 corre **después** de SP1. Se asume que SP1 entregó:

- Dexie `version(2)`; entidades con `guid: string`, `updatedAt: number`, `deletedAt?: number | null`.
- `TaskStatus = "todo" | "in_progress" | "blocked" | "done"` y `Subtask { id; text; done }`.
- Sheet expandido funcionando: **el ejecutor debe localizar el componente sheet creado en SP1** (probablemente `components/entity-sheet.tsx` o equivalente). Si no existe un sheet unificado y cada row mantiene su propio `<Sheet>` (como en el baseline actual `task-row.tsx`/`idea-row.tsx`), el ejecutor integra los componentes de fotos en cada uno de esos sheets.
- Chips de preview con orden `[due][checklist][links][fotos]` ya montados; el chip de fotos es el último.
- vitest + fake-indexeddb configurados (existe `vitest.config.ts` y un setup que importa `fake-indexeddb/auto`).
- `lib/id.ts` con `newGuid()` (UUID v4).

> **Comprobación previa obligatoria (Task 0):** antes de escribir código, el ejecutor verifica estos supuestos y anota las divergencias de nombres. Si algún nombre difiere, **adapta** los imports a lo que exista en el repo.

## Contrato `Photo` (VERBATIM — SP4 depende de estos nombres)

```ts
export interface Photo {
  id?: number;
  guid: string;
  parentType: "task" | "idea" | "backlog";
  parentGuid: string;
  blob: Blob;        // WebP, lado mayor <=1600px, q~0.8
  thumb: Blob;       // WebP, lado mayor ~200px
  width: number;     // del blob principal
  height: number;
  createdAt: number;
  updatedAt: number;
  deletedAt?: number | null;
  remoteUrl?: string | null;  // null hasta SP4
}
```

Índice Dexie `version(3)`: `photos: "++id, &guid, parentGuid, updatedAt"`.

## File Structure

```
lib/
  types.ts                      # (editar) export interface Photo + PhotoParentType
  db/
    schema.ts                   # (editar) version(3) + tabla photos
    photos.ts                   # (nuevo) helpers de datos de fotos
  image/
    compress.ts                 # (nuevo) fitDimensions (puro) + compressImage (canvas)
    object-url.ts               # (nuevo) gestor de object URLs con revoke
  use-paste-images.ts           # (nuevo) hook de paste de portapapeles
components/
  photo-attach-button.tsx       # (nuevo) picker + cámara
  photo-chip.tsx                # (nuevo) chip Image + contador (preview)
  photo-grid.tsx                # (nuevo) grid de thumbnails (sheet)
  photo-viewer.tsx              # (nuevo) viewer fullscreen
  task-row.tsx                  # (editar) chip + grid + paste + promote
  idea-row.tsx                  # (editar) chip + grid + paste + promote-reparent
  quick-add.tsx                 # (editar) attach + paste en alta
lib/db/
  queries.ts                    # (editar) promoteIdeaToTask re-parenta fotos; deletes tombstonean fotos
  photos.test.ts                # (nuevo) tests de helpers de datos
lib/image/
  compress.test.ts              # (nuevo) tests de fitDimensions + compressImage mockeado
```

---

## Task 0: Verificación de prerrequisitos SP1

**Files:** (solo lectura)

- [ ] Confirmar que existe la versión 2 de Dexie y los campos de sync. Ejecutar:
  ```bash
  grep -n "version(2)" lib/db/schema.ts
  grep -n "guid" lib/types.ts
  grep -n "deletedAt" lib/types.ts
  ```
  Salida esperada: al menos una coincidencia en cada grep. Si `version(2)` no existe, **detenerse**: SP1 no está integrado.

- [ ] Confirmar `newGuid()`:
  ```bash
  grep -n "newGuid" lib/id.ts
  ```
  Salida esperada: una línea `export function newGuid()` (o similar). Anotar el nombre real si difiere.

- [ ] Localizar el sheet de SP1 y el orden de chips:
  ```bash
  ls components/entity-sheet.tsx 2>/dev/null || echo "NO entity-sheet — usar sheets por-row"
  grep -rn "checklist\|links\|due" components/task-row.tsx | head
  ```
  Anotar: ¿hay sheet unificado o sheets por row? El ejecutor integrará en consecuencia.

- [ ] Confirmar vitest + fake-indexeddb:
  ```bash
  cat vitest.config.ts 2>/dev/null && grep -rn "fake-indexeddb" vitest.setup.ts vitest.config.ts 2>/dev/null
  pnpm vitest run 2>&1 | tail -5
  ```
  Salida esperada: la suite de SP1 corre verde. Si no hay vitest configurado, **detenerse**: SP1 no está integrado.

- [ ] Escribir en el cuerpo del commit de la Task 1 cualquier divergencia de nombres encontrada.

**No hay commit en esta task** (solo verificación).

---

## Task 1: Tipo `Photo` y tabla Dexie `version(3)`

**Files:** `lib/types.ts`, `lib/db/schema.ts`

- [ ] (1) Añadir el tipo `Photo` al final de `lib/types.ts`, VERBATIM:
  ```ts
  export type PhotoParentType = "task" | "idea" | "backlog";

  export interface Photo {
    id?: number;
    guid: string;
    parentType: PhotoParentType;
    parentGuid: string;
    blob: Blob; // WebP, lado mayor <=1600px, q~0.8
    thumb: Blob; // WebP, lado mayor ~200px
    width: number; // del blob principal
    height: number;
    createdAt: number;
    updatedAt: number;
    deletedAt?: number | null;
    remoteUrl?: string | null; // null hasta SP4
  }
  ```

- [ ] (2) En `lib/db/schema.ts`, importar `Photo` y declarar la tabla. Editar el import:
  ```ts
  import type { BacklogItem, Idea, Photo, Project, Task } from "@/lib/types";
  ```
  Añadir la propiedad de tabla dentro de la clase `CadenceDB`, junto a las demás:
  ```ts
  photos!: Table<Photo, number>;
  ```

- [ ] (3) Añadir `version(3)` tras el bloque `version(2)` de SP1 (NO modificar version(1) ni version(2)). El bloque queda así:
  ```ts
  this.version(3).stores({
    photos: "++id, &guid, parentGuid, updatedAt",
  });
  ```
  > Nota Dexie: `version(3).stores` solo lista tablas nuevas/cambiadas; las tablas existentes se heredan. No repetir `projects/tasks/...` aquí.

- [ ] (4) Verificar tipos y lint:
  ```bash
  pnpm exec tsc --noEmit
  pnpm lint
  ```
  Salida esperada: sin errores.

- [ ] (5) Commit:
  ```bash
  git add lib/types.ts lib/db/schema.ts
  git commit -m "feat(sp2): Photo type + Dexie version(3) photos table"
  ```

---

## Task 2: Helpers de datos de fotos (TDD)

**Files:** `lib/db/photos.test.ts` (nuevo), `lib/db/photos.ts` (nuevo)

- [ ] (1) Escribir el test PRIMERO. Crear `lib/db/photos.test.ts`:
  ```ts
  import { beforeEach, describe, expect, it } from "vitest";
  import { db } from "@/lib/db/schema";
  import {
    addPhoto,
    listPhotos,
    tombstonePhoto,
    reparentPhotos,
    tombstonePhotosForParent,
  } from "@/lib/db/photos";

  function fakeBlob(text = "x"): Blob {
    return new Blob([text], { type: "image/webp" });
  }

  beforeEach(async () => {
    await db.delete();
    await db.open();
  });

  describe("addPhoto", () => {
    it("persists a photo with a guid and timestamps", async () => {
      const id = await addPhoto({
        parentType: "task",
        parentGuid: "task-1",
        blob: fakeBlob("full"),
        thumb: fakeBlob("thumb"),
        width: 1600,
        height: 1200,
      });
      const row = await db.photos.get(id);
      expect(row).toBeDefined();
      expect(row!.guid).toMatch(/.+/);
      expect(row!.parentGuid).toBe("task-1");
      expect(row!.width).toBe(1600);
      expect(row!.deletedAt).toBeNull();
      expect(row!.remoteUrl).toBeNull();
      expect(row!.createdAt).toBeGreaterThan(0);
      expect(row!.updatedAt).toBe(row!.createdAt);
    });
  });

  describe("listPhotos", () => {
    it("returns only live photos for a parent, oldest first", async () => {
      await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await addPhoto({ parentType: "task", parentGuid: "other", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      const list = await listPhotos("p1");
      expect(list).toHaveLength(2);
      expect(list[0].createdAt).toBeLessThanOrEqual(list[1].createdAt);
    });

    it("excludes tombstoned photos", async () => {
      const id = await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await tombstonePhoto(id);
      expect(await listPhotos("p1")).toHaveLength(0);
    });
  });

  describe("tombstonePhoto", () => {
    it("sets deletedAt and bumps updatedAt without physical delete", async () => {
      const id = await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await tombstonePhoto(id);
      const row = await db.photos.get(id);
      expect(row).toBeDefined();
      expect(row!.deletedAt).toBeGreaterThan(0);
    });
  });

  describe("reparentPhotos", () => {
    it("moves live photos to a new parent and type, bumping updatedAt", async () => {
      const id = await addPhoto({ parentType: "idea", parentGuid: "idea-1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      const before = (await db.photos.get(id))!.updatedAt;
      await new Promise((r) => setTimeout(r, 2));
      await reparentPhotos("idea-1", "task", "task-9");
      const row = (await db.photos.get(id))!;
      expect(row.parentType).toBe("task");
      expect(row.parentGuid).toBe("task-9");
      expect(row.updatedAt).toBeGreaterThan(before);
      expect(await listPhotos("idea-1")).toHaveLength(0);
      expect(await listPhotos("task-9")).toHaveLength(1);
    });

    it("does not move tombstoned photos", async () => {
      const id = await addPhoto({ parentType: "idea", parentGuid: "idea-1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await tombstonePhoto(id);
      await reparentPhotos("idea-1", "task", "task-9");
      const row = (await db.photos.get(id))!;
      expect(row.parentGuid).toBe("idea-1");
    });
  });

  describe("tombstonePhotosForParent", () => {
    it("tombstones every live photo of a parent", async () => {
      await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await addPhoto({ parentType: "task", parentGuid: "p1", blob: fakeBlob(), thumb: fakeBlob(), width: 10, height: 10 });
      await tombstonePhotosForParent("p1");
      expect(await listPhotos("p1")).toHaveLength(0);
      expect(await db.photos.where("parentGuid").equals("p1").count()).toBe(2);
    });
  });
  ```

- [ ] (2) Correr el test y verlo FALLAR (rojo) por módulo inexistente:
  ```bash
  pnpm vitest run lib/db/photos.test.ts 2>&1 | tail -15
  ```
  Salida esperada: error de resolución de `@/lib/db/photos` o tests en rojo.

- [ ] (3) Implementar `lib/db/photos.ts` para pasar el test:
  ```ts
  import { db } from "@/lib/db/schema";
  import { newGuid } from "@/lib/id";
  import type { Photo, PhotoParentType } from "@/lib/types";

  export interface AddPhotoInput {
    parentType: PhotoParentType;
    parentGuid: string;
    blob: Blob;
    thumb: Blob;
    width: number;
    height: number;
  }

  /** Insert a fully-formed photo row. The binary lives only here. */
  export async function addPhoto(input: AddPhotoInput): Promise<number> {
    const now = Date.now();
    return db.photos.add({
      guid: newGuid(),
      parentType: input.parentType,
      parentGuid: input.parentGuid,
      blob: input.blob,
      thumb: input.thumb,
      width: input.width,
      height: input.height,
      createdAt: now,
      updatedAt: now,
      deletedAt: null,
      remoteUrl: null,
    });
  }

  /** Live photos for a parent, oldest first. */
  export async function listPhotos(parentGuid: string): Promise<Photo[]> {
    const rows = await db.photos.where("parentGuid").equals(parentGuid).toArray();
    return rows
      .filter((p) => p.deletedAt == null)
      .sort((a, b) => a.createdAt - b.createdAt);
  }

  /** Soft-delete (tombstone) a single photo. */
  export async function tombstonePhoto(id: number): Promise<void> {
    const now = Date.now();
    await db.photos.update(id, { deletedAt: now, updatedAt: now });
  }

  /**
   * Move every live photo from one parent to another (idea -> task on promote).
   * Tombstoned photos are left untouched.
   */
  export async function reparentPhotos(
    fromParentGuid: string,
    toParentType: PhotoParentType,
    toParentGuid: string,
  ): Promise<void> {
    const now = Date.now();
    await db.transaction("rw", db.photos, async () => {
      const rows = await db.photos
        .where("parentGuid")
        .equals(fromParentGuid)
        .toArray();
      await Promise.all(
        rows
          .filter((p) => p.deletedAt == null)
          .map((p) =>
            db.photos.update(p.id!, {
              parentType: toParentType,
              parentGuid: toParentGuid,
              updatedAt: now,
            }),
          ),
      );
    });
  }

  /** Tombstone all live photos of a parent (parent entity deleted). */
  export async function tombstonePhotosForParent(
    parentGuid: string,
  ): Promise<void> {
    const now = Date.now();
    await db.transaction("rw", db.photos, async () => {
      const rows = await db.photos
        .where("parentGuid")
        .equals(parentGuid)
        .toArray();
      await Promise.all(
        rows
          .filter((p) => p.deletedAt == null)
          .map((p) =>
            db.photos.update(p.id!, { deletedAt: now, updatedAt: now }),
          ),
      );
    });
  }
  ```

- [ ] (4) Correr el test y verlo PASAR (verde):
  ```bash
  pnpm vitest run lib/db/photos.test.ts 2>&1 | tail -10
  ```
  Salida esperada: todos los tests verdes.

- [ ] (5) Commit:
  ```bash
  git add lib/db/photos.ts lib/db/photos.test.ts
  git commit -m "feat(sp2): photo data helpers (add/list/tombstone/reparent) + tests"
  ```

---

## Task 3: Pipeline de compresión — dimensiones puras + canvas (TDD)

**Files:** `lib/image/compress.test.ts` (nuevo), `lib/image/compress.ts` (nuevo)

- [ ] (1) Escribir el test PRIMERO. Crear `lib/image/compress.test.ts`:
  ```ts
  import { describe, expect, it, vi } from "vitest";
  import { fitDimensions } from "@/lib/image/compress";

  describe("fitDimensions", () => {
    it("leaves images smaller than max untouched", () => {
      expect(fitDimensions(800, 600, 1600)).toEqual({ width: 800, height: 600 });
    });

    it("scales a landscape image so the longer side equals max", () => {
      expect(fitDimensions(3200, 1600, 1600)).toEqual({ width: 1600, height: 800 });
    });

    it("scales a portrait image so the longer side equals max", () => {
      expect(fitDimensions(1600, 3200, 1600)).toEqual({ width: 800, height: 1600 });
    });

    it("rounds to integer pixels", () => {
      const { width, height } = fitDimensions(1000, 333, 200);
      expect(Number.isInteger(width)).toBe(true);
      expect(Number.isInteger(height)).toBe(true);
      expect(width).toBe(200);
      expect(height).toBe(67);
    });

    it("handles square images", () => {
      expect(fitDimensions(2000, 2000, 200)).toEqual({ width: 200, height: 200 });
    });
  });
  ```

- [ ] (2) Correr y ver FALLAR:
  ```bash
  pnpm vitest run lib/image/compress.test.ts 2>&1 | tail -15
  ```
  Salida esperada: error de resolución de `@/lib/image/compress`.

- [ ] (3) Implementar `lib/image/compress.ts`. La parte pura (`fitDimensions`) y la impura (`compressImage`) viven juntas pero la impura está aislada para que jsdom no la ejecute en tests:
  ```ts
  export interface Dimensions {
    width: number;
    height: number;
  }

  export interface CompressedImage {
    blob: Blob;
    thumb: Blob;
    width: number;
    height: number;
  }

  export const MAX_SIDE = 1600;
  export const THUMB_SIDE = 200;
  export const QUALITY = 0.8;

  /**
   * Pure resize math: scale (w,h) so the longer side is at most `max`,
   * preserving aspect ratio. Never upscales. Rounds to whole pixels.
   */
  export function fitDimensions(w: number, h: number, max: number): Dimensions {
    const longest = Math.max(w, h);
    if (longest <= max) return { width: Math.round(w), height: Math.round(h) };
    const scale = max / longest;
    return { width: Math.round(w * scale), height: Math.round(h * scale) };
  }

  /**
   * Draw a bitmap onto an offscreen canvas at the given size and encode WebP.
   * Impure: depends on OffscreenCanvas/createImageBitmap. Not run under jsdom.
   */
  async function renderWebp(
    bitmap: ImageBitmap,
    dims: Dimensions,
    quality: number,
  ): Promise<Blob> {
    // OffscreenCanvas where available (Chrome/Android/desktop); fall back to
    // a detached <canvas> for Safari, which lacks convertToBlob.
    if (typeof OffscreenCanvas !== "undefined") {
      const canvas = new OffscreenCanvas(dims.width, dims.height);
      const ctx = canvas.getContext("2d");
      if (!ctx) throw new Error("2d context unavailable");
      ctx.drawImage(bitmap, 0, 0, dims.width, dims.height);
      return canvas.convertToBlob({ type: "image/webp", quality });
    }
    const canvas = document.createElement("canvas");
    canvas.width = dims.width;
    canvas.height = dims.height;
    const ctx = canvas.getContext("2d");
    if (!ctx) throw new Error("2d context unavailable");
    ctx.drawImage(bitmap, 0, 0, dims.width, dims.height);
    return new Promise<Blob>((resolve, reject) => {
      canvas.toBlob(
        (b) => (b ? resolve(b) : reject(new Error("toBlob returned null"))),
        "image/webp",
        quality,
      );
    });
  }

  /**
   * Compress a user-supplied image file into a WebP main blob (longer side
   * <= MAX_SIDE, q~0.8) plus a WebP thumbnail (longer side ~THUMB_SIDE).
   * Impure: depends on createImageBitmap. Verify manually (see plan Task 9).
   */
  export async function compressImage(file: Blob): Promise<CompressedImage> {
    const bitmap = await createImageBitmap(file);
    try {
      const full = fitDimensions(bitmap.width, bitmap.height, MAX_SIDE);
      const thumbDims = fitDimensions(bitmap.width, bitmap.height, THUMB_SIDE);
      const [blob, thumb] = await Promise.all([
        renderWebp(bitmap, full, QUALITY),
        renderWebp(bitmap, thumbDims, QUALITY),
      ]);
      return { blob, thumb, width: full.width, height: full.height };
    } finally {
      bitmap.close();
    }
  }
  ```

- [ ] (4) Correr y ver PASAR `fitDimensions`:
  ```bash
  pnpm vitest run lib/image/compress.test.ts 2>&1 | tail -10
  ```
  Salida esperada: 5 tests verdes.

- [ ] (5) Añadir un test de `compressImage` con `createImageBitmap`/`OffscreenCanvas` mockeados, para fijar el contrato (qué llama y qué devuelve) sin requerir un canvas real. Añadir AL FINAL de `lib/image/compress.test.ts`:
  ```ts
  import { compressImage } from "@/lib/image/compress";

  describe("compressImage (mocked canvas)", () => {
    it("produces a main blob and a thumb, using fitDimensions for sizing", async () => {
      const close = vi.fn();
      vi.stubGlobal(
        "createImageBitmap",
        vi.fn(async () => ({ width: 3200, height: 1600, close }) as unknown as ImageBitmap),
      );
      const convertToBlob = vi.fn(async () => new Blob(["webp"], { type: "image/webp" }));
      const drawImage = vi.fn();
      class FakeOffscreen {
        constructor(public width: number, public height: number) {}
        getContext() {
          return { drawImage } as unknown as OffscreenCanvasRenderingContext2D;
        }
        convertToBlob = convertToBlob;
      }
      vi.stubGlobal("OffscreenCanvas", FakeOffscreen as unknown as typeof OffscreenCanvas);

      const result = await compressImage(new Blob(["src"], { type: "image/png" }));

      expect(result.width).toBe(1600); // 3200 longer side -> 1600
      expect(result.height).toBe(800);
      expect(result.blob).toBeInstanceOf(Blob);
      expect(result.thumb).toBeInstanceOf(Blob);
      expect(convertToBlob).toHaveBeenCalledTimes(2); // full + thumb
      expect(close).toHaveBeenCalledOnce();

      vi.unstubAllGlobals();
    });
  });
  ```

- [ ] (6) Correr toda la suite del archivo y ver verde:
  ```bash
  pnpm vitest run lib/image/compress.test.ts 2>&1 | tail -10
  ```
  Salida esperada: 6 tests verdes.

- [ ] (7) Commit:
  ```bash
  git add lib/image/compress.ts lib/image/compress.test.ts
  git commit -m "feat(sp2): WebP compression pipeline (pure fitDimensions + canvas) + tests"
  ```

---

## Task 4: Gestión de object URLs + hook `usePhotoUrls`

**Files:** `lib/image/object-url.ts` (nuevo)

- [ ] (1) Crear `lib/image/object-url.ts` con un hook que crea object URLs para una lista de blobs y los revoca al cambiar o desmontar (evita fugas de memoria):
  ```ts
  "use client";

  import { useEffect, useState } from "react";

  /**
   * Create object URLs for a list of blobs and revoke them on change/unmount.
   * Returns URLs aligned by index with `blobs`. Revoking is the whole point:
   * un-revoked object URLs leak until the document is discarded.
   */
  export function useObjectUrls(blobs: Blob[]): string[] {
    const [urls, setUrls] = useState<string[]>([]);

    useEffect(() => {
      const created = blobs.map((b) => URL.createObjectURL(b));
      setUrls(created);
      return () => {
        for (const u of created) URL.revokeObjectURL(u);
      };
      // Re-run when the set of blobs changes identity/length.
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [blobs]);

    return urls;
  }
  ```

- [ ] (2) Verificar tipos/lint:
  ```bash
  pnpm exec tsc --noEmit && pnpm lint
  ```
  Salida esperada: sin errores.

- [ ] (3) Commit:
  ```bash
  git add lib/image/object-url.ts
  git commit -m "feat(sp2): useObjectUrls hook with revoke on unmount"
  ```

---

## Task 5: Botón de captura (picker + cámara) y hook de paste

**Files:** `components/photo-attach-button.tsx` (nuevo), `lib/use-paste-images.ts` (nuevo)

- [ ] (1) Crear `components/photo-attach-button.tsx`. Expone dos entradas: picker general y cámara (`capture="environment"`). Comprime cada archivo y llama `onAttach` con los datos comprimidos listos para `addPhoto`:
  ```tsx
  "use client";

  import { useRef } from "react";
  import { Camera, ImagePlus } from "lucide-react";
  import { compressImage, type CompressedImage } from "@/lib/image/compress";

  export function PhotoAttachButton({
    onAttach,
    disabled,
  }: {
    onAttach: (img: CompressedImage) => void | Promise<void>;
    disabled?: boolean;
  }) {
    const pickerRef = useRef<HTMLInputElement>(null);
    const cameraRef = useRef<HTMLInputElement>(null);

    async function handleFiles(files: FileList | null) {
      if (!files) return;
      for (const file of Array.from(files)) {
        if (!file.type.startsWith("image/")) continue;
        const img = await compressImage(file);
        await onAttach(img);
      }
    }

    return (
      <div className="flex items-center gap-1.5">
        <button
          type="button"
          disabled={disabled}
          onClick={() => pickerRef.current?.click()}
          aria-label="Add photo"
          className="inline-flex items-center gap-1.5 rounded-full border border-border px-2.5 py-1 text-[12px] text-muted transition hover:text-foreground disabled:opacity-50"
        >
          <ImagePlus size={14} /> Photo
        </button>
        <button
          type="button"
          disabled={disabled}
          onClick={() => cameraRef.current?.click()}
          aria-label="Take photo"
          className="grid h-7 w-7 place-items-center rounded-full border border-border text-muted transition hover:text-foreground disabled:opacity-50 md:hidden"
        >
          <Camera size={14} />
        </button>
        <input
          ref={pickerRef}
          type="file"
          accept="image/*"
          multiple
          hidden
          onChange={(e) => {
            void handleFiles(e.target.files);
            e.target.value = "";
          }}
        />
        <input
          ref={cameraRef}
          type="file"
          accept="image/*"
          capture="environment"
          hidden
          onChange={(e) => {
            void handleFiles(e.target.files);
            e.target.value = "";
          }}
        />
      </div>
    );
  }
  ```

- [ ] (2) Crear `lib/use-paste-images.ts`. Engancha `paste` en `window` mientras `enabled` es true, extrae imágenes de `clipboardData.items`, comprime y llama `onAttach`:
  ```ts
  "use client";

  import { useEffect } from "react";
  import { compressImage, type CompressedImage } from "@/lib/image/compress";

  /**
   * While `enabled`, intercept clipboard paste of images (screenshots, copied
   * pictures) and hand each compressed result to `onAttach`.
   */
  export function usePasteImages(
    enabled: boolean,
    onAttach: (img: CompressedImage) => void | Promise<void>,
  ): void {
    useEffect(() => {
      if (!enabled) return;
      async function onPaste(e: ClipboardEvent) {
        const items = e.clipboardData?.items;
        if (!items) return;
        const files: File[] = [];
        for (const item of Array.from(items)) {
          if (item.kind === "file" && item.type.startsWith("image/")) {
            const f = item.getAsFile();
            if (f) files.push(f);
          }
        }
        if (files.length === 0) return;
        e.preventDefault();
        for (const f of files) {
          const img = await compressImage(f);
          await onAttach(img);
        }
      }
      window.addEventListener("paste", onPaste);
      return () => window.removeEventListener("paste", onPaste);
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [enabled]);
  }
  ```

- [ ] (3) Verificar tipos/lint:
  ```bash
  pnpm exec tsc --noEmit && pnpm lint
  ```
  Salida esperada: sin errores.

- [ ] (4) Commit:
  ```bash
  git add components/photo-attach-button.tsx lib/use-paste-images.ts
  git commit -m "feat(sp2): photo attach button (picker+camera) + paste hook"
  ```

---

## Task 6: Chip de preview y grid de thumbnails

**Files:** `components/photo-chip.tsx` (nuevo), `components/photo-grid.tsx` (nuevo)

- [ ] (1) Crear `components/photo-chip.tsx` — chip único `Image` + contador para la preview de fila (icono lucide `Image`, renombrado `ImageIcon` para no chocar con el global `Image`):
  ```tsx
  import { Image as ImageIcon } from "lucide-react";

  /** Single preview chip: photo icon + count. Hidden when count is 0. */
  export function PhotoChip({ count }: { count: number }) {
    if (count <= 0) return null;
    return (
      <span className="inline-flex items-center gap-1 rounded-md bg-border/60 px-1.5 py-0.5 text-[11px] text-muted">
        <ImageIcon size={11} /> {count}
      </span>
    );
  }
  ```

- [ ] (2) Crear `components/photo-grid.tsx` — grid de thumbnails dentro del sheet. Usa `useObjectUrls` sobre los `thumb` y abre el viewer en el índice tocado:
  ```tsx
  "use client";

  import { useMemo, useState } from "react";
  import type { Photo } from "@/lib/types";
  import { useObjectUrls } from "@/lib/image/object-url";
  import { PhotoViewer } from "./photo-viewer";

  export function PhotoGrid({ photos }: { photos: Photo[] }) {
    const thumbs = useMemo(() => photos.map((p) => p.thumb), [photos]);
    const urls = useObjectUrls(thumbs);
    const [viewerIndex, setViewerIndex] = useState<number | null>(null);

    if (photos.length === 0) return null;

    return (
      <>
        <div className="mt-4 grid grid-cols-4 gap-1.5">
          {photos.map((p, i) => (
            <button
              key={p.guid}
              type="button"
              onClick={() => setViewerIndex(i)}
              className="aspect-square overflow-hidden rounded-lg border border-border bg-border/30"
              aria-label={`Open photo ${i + 1}`}
            >
              {urls[i] && (
                // eslint-disable-next-line @next/next/no-img-element
                <img
                  src={urls[i]}
                  alt=""
                  className="h-full w-full object-cover"
                />
              )}
            </button>
          ))}
        </div>
        {viewerIndex !== null && (
          <PhotoViewer
            photos={photos}
            index={viewerIndex}
            onIndexChange={setViewerIndex}
            onClose={() => setViewerIndex(null)}
          />
        )}
      </>
    );
  }
  ```

- [ ] (3) Verificar tipos/lint (el viewer aún no existe; este paso fallará hasta la Task 7 — saltar `tsc` hasta entonces o crear un stub temporal). Para no romper el commit, crear primero el viewer en la Task 7 y verificar tipos al final de ella. Commit de este task SOLO tras Task 7 verde. **Marcar en TODO interno y continuar a Task 7.**

> Nota de orden: las Tasks 6 y 7 son interdependientes (grid abre viewer). Implementar ambas y luego un único commit. Mantener pasos separados por claridad.

---

## Task 7: Viewer a pantalla completa (swipe + flechas + borrar)

**Files:** `components/photo-viewer.tsx` (nuevo); luego commit conjunto con Task 6.

- [ ] (1) Crear `components/photo-viewer.tsx`. Viewer fullscreen con Radix Dialog: muestra el blob principal, navega con flechas (desktop) y swipe (móvil), y borra (tombstone) la foto actual:
  ```tsx
  "use client";

  import { useEffect, useMemo, useRef } from "react";
  import * as Dialog from "@radix-ui/react-dialog";
  import { ChevronLeft, ChevronRight, Trash2, X } from "lucide-react";
  import type { Photo } from "@/lib/types";
  import { useObjectUrls } from "@/lib/image/object-url";
  import { tombstonePhoto } from "@/lib/db/photos";

  export function PhotoViewer({
    photos,
    index,
    onIndexChange,
    onClose,
  }: {
    photos: Photo[];
    index: number;
    onIndexChange: (i: number) => void;
    onClose: () => void;
  }) {
    const blobs = useMemo(() => photos.map((p) => p.blob), [photos]);
    const urls = useObjectUrls(blobs);
    const touchX = useRef<number | null>(null);

    const count = photos.length;
    const go = (delta: number) => {
      if (count === 0) return;
      onIndexChange((index + delta + count) % count);
    };

    useEffect(() => {
      function onKey(e: KeyboardEvent) {
        if (e.key === "ArrowLeft") go(-1);
        else if (e.key === "ArrowRight") go(1);
      }
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [index, count]);

    async function remove() {
      const current = photos[index];
      await tombstonePhoto(current.id!);
      if (count <= 1) {
        onClose();
        return;
      }
      // Stay in range after removal; the live list will shrink on re-render.
      onIndexChange(Math.min(index, count - 2));
    }

    return (
      <Dialog.Root open onOpenChange={(o) => !o && onClose()}>
        <Dialog.Portal>
          <Dialog.Overlay className="fixed inset-0 z-[60] bg-black/90" />
          <Dialog.Content
            className="fixed inset-0 z-[70] flex flex-col focus:outline-none"
            onTouchStart={(e) => (touchX.current = e.touches[0].clientX)}
            onTouchEnd={(e) => {
              if (touchX.current === null) return;
              const dx = e.changedTouches[0].clientX - touchX.current;
              if (Math.abs(dx) > 40) go(dx < 0 ? 1 : -1);
              touchX.current = null;
            }}
          >
            <Dialog.Title className="sr-only">Photo viewer</Dialog.Title>
            <div className="flex items-center justify-between p-4 text-white">
              <span className="text-[13px] opacity-80">
                {index + 1} / {count}
              </span>
              <div className="flex items-center gap-2">
                <button
                  type="button"
                  onClick={() => void remove()}
                  aria-label="Delete photo"
                  className="grid h-9 w-9 place-items-center rounded-full bg-white/10 transition hover:bg-white/20"
                >
                  <Trash2 size={18} />
                </button>
                <Dialog.Close
                  aria-label="Close"
                  className="grid h-9 w-9 place-items-center rounded-full bg-white/10 transition hover:bg-white/20"
                >
                  <X size={18} />
                </Dialog.Close>
              </div>
            </div>

            <div className="relative flex flex-1 items-center justify-center overflow-hidden">
              {urls[index] && (
                // eslint-disable-next-line @next/next/no-img-element
                <img
                  src={urls[index]}
                  alt=""
                  className="max-h-full max-w-full object-contain"
                />
              )}
              {count > 1 && (
                <>
                  <button
                    type="button"
                    onClick={() => go(-1)}
                    aria-label="Previous photo"
                    className="absolute left-2 hidden h-11 w-11 place-items-center rounded-full bg-white/10 text-white transition hover:bg-white/20 md:grid"
                  >
                    <ChevronLeft size={22} />
                  </button>
                  <button
                    type="button"
                    onClick={() => go(1)}
                    aria-label="Next photo"
                    className="absolute right-2 hidden h-11 w-11 place-items-center rounded-full bg-white/10 text-white transition hover:bg-white/20 md:grid"
                  >
                    <ChevronRight size={22} />
                  </button>
                </>
              )}
            </div>
          </Dialog.Content>
        </Dialog.Portal>
      </Dialog.Root>
    );
  }
  ```

- [ ] (2) Verificar tipos/lint de las Tasks 6 + 7 juntas:
  ```bash
  pnpm exec tsc --noEmit && pnpm lint
  ```
  Salida esperada: sin errores.

- [ ] (3) Commit conjunto Tasks 6 + 7:
  ```bash
  git add components/photo-chip.tsx components/photo-grid.tsx components/photo-viewer.tsx
  git commit -m "feat(sp2): photo chip, thumbnail grid, fullscreen viewer (swipe/arrows/delete)"
  ```

---

## Task 8: Integración en quick-add, rows y queries (re-parent + tombstone en cascada)

**Files:** `components/quick-add.tsx`, `components/task-row.tsx`, `components/idea-row.tsx`, el row/sheet de backlog (localizar: probablemente integrado en una vista de backlog), `lib/db/queries.ts`

> El ejecutor adapta a la realidad de SP1: si existe un `entity-sheet.tsx` unificado, integra ahí una sola vez en lugar de en cada row.

### 8a — Queries: re-parent al promover y tombstone en cascada al borrar

- [ ] (1) En `lib/db/queries.ts`, importar los helpers de fotos:
  ```ts
  import { reparentPhotos, tombstonePhotosForParent } from "@/lib/db/photos";
  ```

- [ ] (2) En `promoteIdeaToTask`, tras crear la task y antes de cerrar la transacción, re-parentar las fotos de la idea a la nueva task. Esto requiere el `guid` de la idea y de la task nueva. Ajustar `addTask` para devolver/conocer el `guid` de la task creada (SP1 ya genera guids; si `addTask` no expone el guid, leer la task recién creada por su id). Implementación de referencia (adaptar a la firma real de SP1):
  ```ts
  export async function promoteIdeaToTask(idea: Idea, bucket: Bucket): Promise<void> {
    await db.transaction("rw", db.tasks, db.ideas, db.photos, async () => {
      const taskId = await addTask({ title: idea.text, projectId: idea.projectId, bucket });
      const task = await db.tasks.get(taskId);
      await db.ideas.update(idea.id!, { status: "promoted" });
      if (task && idea.guid) {
        await reparentPhotos(idea.guid, "task", task.guid);
      }
    });
  }
  ```
  > Si `db.transaction` de SP1 no incluye `db.photos` en su lista de tablas, AÑADIRLO (como arriba); de lo contrario la operación lanzará por tabla no declarada.

- [ ] (3) En cada función de borrado de entidad (`deleteTask`, `deleteIdea`, `deleteBacklog`), tombstonear las fotos del padre. Importante: SP1 convirtió los deletes en tombstones; el patrón es marcar `deletedAt` en la entidad y además llamar `tombstonePhotosForParent(entity.guid)`. Como estas funciones reciben `id` numérico, primero leer la entidad para obtener su `guid`. Ejemplo para tasks (replicar análogamente en ideas y backlog):
  ```ts
  export async function deleteTask(id: number): Promise<void> {
    const task = await db.tasks.get(id);
    await db.transaction("rw", db.tasks, db.photos, async () => {
      // SP1 tombstone pattern for the entity itself:
      await db.tasks.update(id, { deletedAt: Date.now(), updatedAt: Date.now() });
      if (task?.guid) await tombstonePhotosForParent(task.guid);
    });
  }
  ```
  > Adaptar al patrón de tombstone exacto de SP1. Si SP1 ya centralizó el borrado en un helper genérico, enganchar `tombstonePhotosForParent` ahí una sola vez.

- [ ] (4) Verificar tipos/lint:
  ```bash
  pnpm exec tsc --noEmit && pnpm lint
  ```

### 8b — Quick-add: adjuntar y pegar al crear

- [ ] (5) En `components/quick-add.tsx`, mantener un estado de fotos pendientes (comprimidas, aún sin guid de parent porque la entidad no existe), añadir el botón de attach y el hook de paste, y persistirlas tras crear la task con su `guid`. Cambios clave:
  ```tsx
  import { PhotoAttachButton } from "./photo-attach-button";
  import { usePasteImages } from "@/lib/use-paste-images";
  import { addPhoto } from "@/lib/db/photos";
  import type { CompressedImage } from "@/lib/image/compress";
  import { Image as ImageIcon } from "lucide-react";
  // ...
  const [pending, setPending] = useState<CompressedImage[]>([]);
  usePasteImages(open, (img) => setPending((p) => [...p, img]));
  // dentro de openSheet(): setPending([]);
  ```
  En `submit()`, tras crear la task, leer su guid y persistir cada foto pendiente:
  ```tsx
  async function submit() {
    const { title, links } = extractLinks(text);
    if (!title.trim() && pending.length === 0) return;
    const taskId = await addTask({ title, links, projectId, bucket, time: time || null });
    const task = await db.tasks.get(taskId);
    if (task?.guid) {
      for (const img of pending) {
        await addPhoto({
          parentType: "task",
          parentGuid: task.guid,
          blob: img.blob,
          thumb: img.thumb,
          width: img.width,
          height: img.height,
        });
      }
    }
    setPending([]);
    setOpen(false);
  }
  ```
  En el JSX del sheet, junto a los demás controles, añadir el botón de attach y un contador de pendientes:
  ```tsx
  <div className="mt-3 flex items-center gap-2">
    <PhotoAttachButton onAttach={(img) => setPending((p) => [...p, img])} />
    {pending.length > 0 && (
      <span className="inline-flex items-center gap-1 text-[12px] text-muted">
        <ImageIcon size={12} /> {pending.length} attached
      </span>
    )}
  </div>
  ```

### 8c — Task row: chip en preview + grid + paste + attach en el sheet

- [ ] (6) En `components/task-row.tsx`:
  - Importar y usar `useLiveQuery` para las fotos vivas de la task:
    ```tsx
    import { listPhotos, addPhoto } from "@/lib/db/photos";
    import { PhotoChip } from "./photo-chip";
    import { PhotoGrid } from "./photo-grid";
    import { PhotoAttachButton } from "./photo-attach-button";
    import { usePasteImages } from "@/lib/use-paste-images";
    // ...
    const photos = useLiveQuery(() => listPhotos(task.guid), [task.guid], []);
    usePasteImages(open, async (img) => {
      await addPhoto({ parentType: "task", parentGuid: task.guid, blob: img.blob, thumb: img.thumb, width: img.width, height: img.height });
    });
    ```
  - Insertar `<PhotoChip count={photos.length} />` en la preview **al final** de la lista de chips, respetando el orden `[due][checklist][links][fotos]` que SP1 montó. Ajustar la condición de render del bloque de chips para incluir `photos.length > 0`.
  - En el `SheetContent`, tras los controles existentes, añadir attach + grid:
    ```tsx
    <div className="mt-4 flex items-center gap-2">
      <PhotoAttachButton
        onAttach={(img) =>
          addPhoto({ parentType: "task", parentGuid: task.guid, blob: img.blob, thumb: img.thumb, width: img.width, height: img.height })
        }
      />
    </div>
    <PhotoGrid photos={photos} />
    ```

### 8d — Idea row: igual con `parentType: "idea"`

- [ ] (7) Replicar 8c en `components/idea-row.tsx` con `parentType: "idea"` y `parentGuid: idea.guid`. El re-parent al promover ya lo hace `promoteIdeaToTask` (8a). Insertar el `PhotoChip` en la preview de la idea y el grid/attach en su sheet.

### 8e — Backlog: igual con `parentType: "backlog"`

- [ ] (8) Localizar el row/sheet de backlog (revisar `components/` y la vista de Histórico que renderiza `BacklogItem`). Replicar el patrón con `parentType: "backlog"` y `parentGuid: item.guid`. Si SP1 no añadió `guid` a `BacklogItem`, **detenerse y reportar** (el contrato requiere `parentGuid`).

- [ ] (9) Verificación final de tipos/lint/tests:
  ```bash
  pnpm exec tsc --noEmit
  pnpm lint
  pnpm vitest run 2>&1 | tail -15
  ```
  Salida esperada: sin errores de tipos, lint limpio, toda la suite verde (incluidos los tests de Tasks 2 y 3).

- [ ] (10) Commit:
  ```bash
  git add components/quick-add.tsx components/task-row.tsx components/idea-row.tsx lib/db/queries.ts
  # añadir también el archivo del row/sheet de backlog que se haya editado:
  # git add components/<backlog-row-o-vista>.tsx
  git commit -m "feat(sp2): wire photos into quick-add, task/idea/backlog rows; reparent on promote; cascade tombstone on delete"
  ```

---

## Task 9: Verificación manual (compresión, cámara, paste) y cierre

La compresión real con canvas NO corre en jsdom/node, así que se valida a mano. **Esta task no escribe código**; produce evidencia.

- [ ] (1) Arrancar el dev server:
  ```bash
  pnpm dev
  ```
  Abrir `http://localhost:3000`.

- [ ] (2) **Picker + tamaño (criterio del spec):**
  - Crear una task, abrir su sheet, pulsar "Photo" y elegir una foto real de **~4 MB** (JPEG de cámara).
  - Abrir DevTools → Application → IndexedDB → `cadence` → `photos`. Localizar la fila nueva.
  - Confirmar que `blob.size` ≤ **300 KB** y `type` = `image/webp`; que `width` ≤ 1600; que existe `thumb` (~pocos KB).
  - **Evidencia:** anotar `blob.size` real en bytes.

- [ ] (3) **Chip + contador:** verificar que la fila de la task muestra el chip `Image` con el contador correcto (ej. `1`), en último lugar tras due/checklist/links. Adjuntar una segunda foto y confirmar que el contador pasa a `2`.

- [ ] (4) **Grid + viewer:** en el sheet, el grid muestra thumbnails. Tocar uno abre el viewer fullscreen. En desktop, las flechas izquierda/derecha y las teclas `←`/`→` navegan. Borrar (papelera) reduce el contador y, al borrar la última, cierra el viewer. Confirmar en IndexedDB que la foto borrada tiene `deletedAt` (no se eliminó físicamente).

- [ ] (5) **Paste de screenshot (desktop):** capturar una screenshot al portapapeles, abrir el quick-add (o un sheet) y pulsar `Cmd/Ctrl+V`. La imagen se adjunta. Confirmar persistencia en IndexedDB tras guardar.

- [ ] (6) **Cámara móvil:** exponer el dev server por red:
  ```bash
  pnpm dev --hostname 0.0.0.0
  ```
  Desde el móvil, abrir `http://<IP-LAN>:3000`, crear/abrir una task, pulsar el botón de cámara y tomar una foto con la cámara trasera (`capture="environment"`). Confirmar que se adjunta y comprime.

- [ ] (7) **Re-parent al promover:** crear una idea, adjuntarle una foto, promoverla a task. Confirmar que la foto aparece en la task resultante y desaparece de la idea (en IndexedDB, la fila cambió `parentType`/`parentGuid`).

- [ ] (8) **Tombstone en cascada:** borrar una task con fotos. Confirmar que sus fotos quedan con `deletedAt` (tombstone), no eliminadas físicamente.

- [ ] (9) **Memoria (object URLs):** abrir y cerrar el viewer y varios sheets repetidamente. En DevTools → Memory/Performance, confirmar que no crecen indefinidamente los object URLs (heurística: sin fugas evidentes). El cleanup de `useObjectUrls` debe revocarlos.

- [ ] (10) Cerrar el dev server. Si todo lo anterior pasa, registrar la evidencia (tamaños observados) en el cuerpo del commit final o en el PR. Última verificación agregada:
  ```bash
  pnpm vitest run 2>&1 | tail -5
  pnpm build
  ```
  Salida esperada: tests verdes y build de static export sin errores.

---

## Resumen de cobertura del spec (SP2)

| Requisito del spec | Cubierto en |
|---|---|
| Tabla `photos` con el shape del contrato | Task 1 |
| Dexie `version(3)` índice `++id, &guid, parentGuid, updatedAt` | Task 1 |
| Picker de archivos | Task 5 / 8 |
| Cámara móvil `capture="environment"` | Task 5 / 9.6 |
| Paste de portapapeles (quick-add + sheet) | Task 5 / 8 |
| Compresión WebP ≤1600px q≈0.8 + thumb 200px, 4MB→≤300KB | Task 3 / 9.2 |
| Sin dependencias nuevas (canvas/createImageBitmap) | Task 3 |
| Chip único `Image` + contador en preview | Task 6 / 8 |
| Grid de thumbnails en el sheet | Task 6 / 8 |
| Viewer fullscreen (swipe móvil, flechas desktop, borrar=tombstone) | Task 7 |
| Object URLs con revoke | Task 4 |
| Re-parent al promover idea→task | Task 2 / 8a |
| Borrado de padre tombstonea fotos | Task 2 / 8a |
| Binario nunca en entidad ni en sync relacional | Arquitectura (tabla aparte) |
| TDD helpers DB + dimensiones puras | Tasks 2, 3 |
| Verificación manual de compresión/cámara/paste | Task 9 |
